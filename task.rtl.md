# تسک فنی Go — Ticketing Service

یک سیستم **Ticketing** با زبان **Go** طراحی و پیاده‌سازی کنید.

## معماری کلی

سیستم شامل دو بخش است:

* **BFF:** یک سرویس ساده با **REST API** که تنها وظیفه آن دریافت درخواست‌های کاربر و ارتباط با Ticketing Service است.
* **Ticketing Service:** سرویس اصلی مدیریت تیکت‌ها که تمام ارتباطات آن از طریق **gRPC** انجام می‌شود.

BFF صرفاً به‌عنوان لایه ارتباطی بین کاربر و Ticketing Service عمل می‌کند و نیازی به پیاده‌سازی قابلیت‌هایی مانند Authentication، Token Management، Rate Limiting و موارد مشابه ندارد.

## امکانات Ticketing Service

سیستم باید حداقل امکانات زیر را داشته باشد:

* ایجاد تیکت توسط کاربر
* ثبت یک یا چند پاسخ برای هر تیکت
* اختصاص تیکت به Support
* امکان انتقال تیکت به Support دیگر
* امکان بستن تیکت توسط Support
* مشاهده تیکت‌های مربوط به هر Support
* امکان فیلتر تیکت‌ها، مانند:

  * تیکت‌های باز
  * همه تیکت‌ها
  * تیکت‌های پاسخ‌داده‌شده و بسته‌شده

تیکت‌ها **قابل حذف نیستند** و اطلاعات ثبت‌شده در تیکت و پاسخ‌ها نیز **قابل ویرایش نیستند**.

## قوانین تخصیص تیکت

* تیکت بلافاصله پس از ایجاد، در صورت وجود Support آزاد، باید به یکی از Supportهای آزاد اختصاص داده شود.
* در صورت نبود Support آزاد، تیکت باید در صف قرار بگیرد.
* به محض آزاد شدن Support، تیکت مناسب بعدی از صف باید به او اختصاص داده شود.
* اگر چند Support آزاد باشند، تیکت قدیمی‌تر و منتظرمانده‌تر باید در اولویت قرار بگیرد و به Supportی اختصاص داده شود که مدت زمان بیشتری از آخرین پاسخگویی او گذشته و آزاد بوده است.

برای مثال، اگر سه Support آزاد باشند و یکی ۴ دقیقه، دیگری ۳ دقیقه و دیگری ۱ دقیقه آزاد بوده باشد، Supportی که ۴ دقیقه آزاد بوده در اولویت تخصیص تیکت قرار دارد.

این قانون هم هنگام ایجاد تیکت و هم هنگام تخصیص تیکت‌های موجود در صف باید رعایت شود.

## تست

برای اجزای مهم سیستم، **Unit Test** مناسب نوشته شود؛ به‌خصوص برای منطق مدیریت تیکت، صف و تخصیص تیکت به Support.

## Docker

پروژه باید به‌صورت **Dockerized** قابل اجرا باشد.

## نکات مهم طراحی

* انتخاب پکیج، کتابخانه و فریم‌ورک کاملاً آزاد است.
* استفاده بهینه از منابع سیستم، به‌خصوص **Memory و CPU**، در ارزیابی اهمیت زیادی دارد.
* ساختار پوشه‌ها و انتخاب معماری پروژه (Clean Architecture، Hexagonal، Domain-Driven و...) اهمیتی ندارد.
* **Clean، خوانا و توسعه‌پذیر بودن کد** اهمیت زیادی دارد.
* استفاده از Raw Query یا ORM آزاد است؛ اما نحوه دسترسی به دیتابیس و Queryهای نوشته‌شده اهمیت دارد.
* طراحی مناسب Database و Indexها اهمیت دارد.
* ساختار دیتابیس باید تا حد امکان ساده باشد و انتخاب فیلدها محدودیتی ندارد.
* انتخاب نوع و نسخه ابزارها، دیتابیس و سایر تکنولوژی‌ها آزاد است.

## استفاده از منابع و ابزارهای هوش مصنوعی

استفاده از **AI Agentها برای پیاده‌سازی کد و ساختار پروژه ممنوع است**.

استفاده از منابع دیگر مانند مستندات، YouTube و جستجو در اینترنت، بدون استفاده از ابزارهای هوش مصنوعی، آزاد است.

## مستندات

نوشتن مستندات پروژه و مواردی مانند **OpenAPI / Swagger** الزامی نیست و نداشتن آن امتیاز منفی ندارد؛ اما در صورت ارائه مستندات مناسب، امتیاز مثبت محسوب می‌شود.

## زمان انجام

**شروع:** شنبه ۱۴ شهریور

**Deadline:** پایان روز شنبه ۲۱ شهریور

هدف اصلی این تسک، ارزیابی توانایی طراحی و پیاده‌سازی یک سیستم واقعی با **Go**، نحوه مدیریت هم‌زمانی، منابع سیستم و کیفیت و توسعه‌پذیری کد است.
